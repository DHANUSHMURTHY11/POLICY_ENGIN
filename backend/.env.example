# ═══════════════════════════════════════════════════════════════
# BaikalSphere Policy Engine — Environment Configuration
# ═══════════════════════════════════════════════════════════════

# ── Application ──
APP_NAME=BaikalSphere Policy Engine
APP_ENV=development
DEBUG=true
SECRET_KEY=change-me-in-production
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ── PostgreSQL ──
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/policy_engine

# ── MongoDB ──
MONGODB_URL=mongodb://localhost:27017
MONGODB_DB_NAME=policy_engine

# ── JWT ──
JWT_SECRET=change-me-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60

# ═══════════════════════════════════════════════════════════════
# AI PROVIDER CONFIGURATION
# ═══════════════════════════════════════════════════════════════
# AI_PROVIDER options: openai | gemini | ollama | auto
#   openai  → uses OPENAI_API_KEY + AI_MODEL_OPENAI
#   gemini  → uses GEMINI_API_KEY + AI_MODEL_GEMINI
#   ollama  → uses OLLAMA_BASE_URL + OLLAMA_MODEL (no API key)
#   auto    → cascade: OpenAI → Gemini → Ollama (first available)
#
# AI_STRICT_MODE:
#   true  → no fallback, selected provider must succeed or error
#   false → auto mode allowed, cascade through providers
# ═══════════════════════════════════════════════════════════════

AI_PROVIDER=ollama
AI_STRICT_MODE=true
AI_TEMPERATURE=0.1

# ── OpenAI ──
OPENAI_API_KEY=
AI_MODEL_OPENAI=gpt-4o-mini

# ── Gemini ──
GEMINI_API_KEY=
AI_MODEL_GEMINI=gemini-1.5-pro

# ── Ollama (local LLM) ──
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_MODEL=qwen2.5:7b-instruct-q4_K_M

# ── Email ──
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=noreply@baikalsphere.com

# ── Admin ──
ADMIN_DEFAULT_PASSWORD=Admin@123
